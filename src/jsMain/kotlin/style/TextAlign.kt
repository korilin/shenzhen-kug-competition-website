package style

import org.jetbrains.compose.web.css.*

interface TextAlign {
    companion object {
        const val center = "center"
        const val left = "left"
        const val right = "right"
    }
}
